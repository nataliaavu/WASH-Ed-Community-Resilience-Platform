plugins {
    id "com.android.application"
}

android {
    namespace 'com.example.washed'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.washed"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.9.0" // Keep for flutter compat but no app code in .kt
}
